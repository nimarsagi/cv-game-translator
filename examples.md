# Examples: the contract holding on three real inputs

> **PLACEHOLDER. No runs yet.** This file will hold three real runs, not made-up ones.
>
> **Which runs will fill it:** the author's own life document (being written now) run against **three real job posts** they bring with it, one run per post. The competition asks for "three different inputs of the same kind"; here that means three job posts against one life document.
>
> **Before the runs are copied in here:** the contact lines are removed from the life document. The missing contact then shows the hiding rule working: gone from the game, marked "not in source" on the check sheet.
>
> Nothing here will be invented to fill the gap until then. A made-up life document proves nothing about a real one.

## What each example will show (once filled)
For each of the three runs:
1. **The input:** the job post, and the life document (the same one each time, contact lines removed).
2. **The map:** `map.txt` as Claude wrote it: line numbers and exact pieces only.
3. **The output:** the check sheet's section 1 (the seven fields) and section 2 (requirements left out), plus the `check-result.txt` verdict.
4. **Where to look:** one cut piece beside its full line, one requirement left out, and one life line left out.

## The three check sheets side by side (once filled)
One table, one column per run, one row per field (Name, Contact, Job title, Company, Levels, Roles, Traits), so the shape can be seen holding while the content changes.

| Field | Run 1 | Run 2 | Run 3 |
|---|---|---|---|
| Name | | | |
| Contact | | | |
| Job title | | | |
| Company | | | |
| Levels | | | |
| Roles | | | |
| Traits | | | |
